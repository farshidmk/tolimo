'use client'

import { type ReactNode, createContext, useRef, useContext } from 'react'
import { useStore } from 'zustand'

type Props = {}

const ExamProvider = (props: Props) => {
  
  return (
    <div>ExamProvider</div>
  )
}

export default ExamProvider